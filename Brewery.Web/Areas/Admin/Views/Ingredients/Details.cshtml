@model IngredientViewModel
<h2>@Model.Name</h2>
<hr>

    <hr />
    <div>
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Quantity</th>
                </tr>
            </thead>
            <tbody>
               
                    @Html.DisplayForModel()
                
            </tbody>
        </table>
    </div>
@Html.DisplayFor(r=>r.Id, "Actions")
